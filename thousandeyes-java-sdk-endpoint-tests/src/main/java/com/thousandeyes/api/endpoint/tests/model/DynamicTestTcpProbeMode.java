/*
 * Endpoint Tests API
 *  ## Overview Manage endpoint agent dynamic and scheduled tests using the Endpoint Tests API.
 *
 * The version of the OpenAPI document: 7.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.thousandeyes.api.endpoint.tests.model;

import java.util.Objects;
import com.thousandeyes.api.endpoint.tests.model.TestProbeMode;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import com.thousandeyes.api.endpoint.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2023-12-20T14:43:32.432392Z[Europe/Lisbon]")
public class DynamicTestTcpProbeMode extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(DynamicTestTcpProbeMode.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!DynamicTestTcpProbeMode.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'DynamicTestTcpProbeMode' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<TestProbeMode> adapterTestProbeMode = gson.getDelegateAdapter(this, TypeToken.get(TestProbeMode.class));

            return (TypeAdapter<T>) new TypeAdapter<DynamicTestTcpProbeMode>() {
                @Override
                public void write(JsonWriter out, DynamicTestTcpProbeMode value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `TestProbeMode`
                    if (value.getActualInstance() instanceof TestProbeMode) {
                      JsonElement element = adapterTestProbeMode.toJsonTree((TestProbeMode)value.getActualInstance());
                      elementAdapter.write(out, element);
                      return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: TestProbeMode");
                }

                @Override
                public DynamicTestTcpProbeMode read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize TestProbeMode
                    try {
                      // validate the JSON object to see if any exception is thrown
                      TestProbeMode.validateJsonElement(jsonElement);
                      actualAdapter = adapterTestProbeMode;
                      match++;
                      log.log(Level.FINER, "Input data matches schema 'TestProbeMode'");
                    } catch (Exception e) {
                      // deserialization failed, continue
                      errorMessages.add(String.format("Deserialization for TestProbeMode failed with `%s`.", e.getMessage()));
                      log.log(Level.FINER, "Input data does not match schema 'TestProbeMode'", e);
                    }

                    if (match == 1) {
                        DynamicTestTcpProbeMode ret = new DynamicTestTcpProbeMode();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for DynamicTestTcpProbeMode: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public DynamicTestTcpProbeMode() {
        super("oneOf", Boolean.FALSE);
    }

    public DynamicTestTcpProbeMode(TestProbeMode o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("TestProbeMode", TestProbeMode.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return DynamicTestTcpProbeMode.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * TestProbeMode
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof TestProbeMode) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be TestProbeMode");
    }

    /**
     * Get the actual instance, which can be the following:
     * TestProbeMode
     *
     * @return The actual instance (TestProbeMode)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `TestProbeMode`. If the actual instance is not `TestProbeMode`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `TestProbeMode`
     * @throws ClassCastException if the instance is not `TestProbeMode`
     */
    public TestProbeMode getTestProbeMode() throws ClassCastException {
        return (TestProbeMode)super.getActualInstance();
    }

 /**
  * Validates the JSON Element and throws an exception if issues found
  *
  * @param jsonElement JSON Element
  * @throws IOException if the JSON Element is invalid with respect to DynamicTestTcpProbeMode
  */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
    // validate oneOf schemas one by one
    int validCount = 0;
    ArrayList<String> errorMessages = new ArrayList<>();
    // validate the json string with TestProbeMode
    try {
      TestProbeMode.validateJsonElement(jsonElement);
      validCount++;
    } catch (Exception e) {
      errorMessages.add(String.format("Deserialization for TestProbeMode failed with `%s`.", e.getMessage()));
      // continue to the next one
    }
    if (validCount != 1) {
      throw new IOException(String.format("The JSON string is invalid for DynamicTestTcpProbeMode with oneOf schemas: TestProbeMode. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
    }
  }

 /**
  * Create an instance of DynamicTestTcpProbeMode given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of DynamicTestTcpProbeMode
  * @throws IOException if the JSON string is invalid with respect to DynamicTestTcpProbeMode
  */
  public static DynamicTestTcpProbeMode fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, DynamicTestTcpProbeMode.class);
  }

 /**
  * Convert an instance of DynamicTestTcpProbeMode to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

